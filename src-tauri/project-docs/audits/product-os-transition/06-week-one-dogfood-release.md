# Milestone 06 — Fresh Founder Dogfood and Release Closure

**Date:** 2026-09-19
**Branch at execution:** `codex/arena-dev-temp`
**Predecessor checkpoint:** `40b6eba74768c41446ef2436852c717f1f353b69`
**Scope:** fresh Linux founder-to-Delivery dogfood, provenance correction, and release preflight. This is not market validation.

## Decision

The fresh founder-to-result path is **runtime-proven on Linux behind the Arena-owned authority adapter**. The overall programme is **PARTIAL**, not a claim of universal release readiness: Windows runtime/package evidence, native GUI launch, hostile same-user filesystem isolation, full SSRF isolation, and an external OpenCode installer remain outside the proven scope.

The founder-autonomy answer is therefore **PARTIAL**. Arena autonomously transported research, verification, package assembly, bounded engineering, and independent Delivery verification. One owner decision was required. The remaining manual/environment boundary is release/platform qualification and the owner-authorized product decision itself; there was no manual URL relay, terminal coordination, ordinary library selection, or worker-result copying.

## CURRENT / IMPLEMENTED

- Web-research EvidenceSource provenance now records the production verifier's actual strength: `observation=search_result; OpenCode tools=websearch; webfetch=not_used_in_production_path`. The separate M05D webfetch probe is retained as a capability experiment, not claim-level fetch evidence.
- Arena admits WebDiscovery and KnownSource work through the durable Product OS/TranscriptStore path; researcher output is Unverified and only a distinct current FactVerifier can finalize it.
- OpenCode runtime status requires the qualified version `1.18.31` for the integrated path. A missing or unsupported executable is not silently treated as ready; OpenCode remains an external prerequisite rather than an installer bundled by Arena.
- The owner view retains its calm Delivery detail and now shows the broader journey label `Discover → Decide → Deliver → Release`.

## SOURCE-CONFIRMED

- Product authority, work orders, evidence, owner decisions, Build Package assembly, gates, candidate worktrees, acceptance freeze, independent verification, canonical-checkout detection, and Safe Apply ownership remain Arena-owned source boundaries.
- OpenCode research uses the built-in `plan` role with bounded `websearch` and denies local read/write, shell, webfetch, skills, MCP, and question tools in the integrated research prompts. OpenCode permissions are not an OS sandbox.
- Search results, worker agreement, and source snippets are not market validation or automatic verified facts. Weak, contradicted, stale, or unresolved evidence remains unable to satisfy decision-critical gates.

## RUNTIME-PROVEN

### Fresh founder flow

The ignored integration test `product_os_runtime::tests::real_m06_fresh_founder_dogfood_research_to_delivery` completed with the real installed runtime in **483.26 seconds**:

- founder input: “I want an application that helps university accounting students master difficult topics interactively.”
- project: `m06-fresh-accounting-interactive-study`;
- final reopened authority revision: `32`;
- research work orders: `7`;
- independently verified claims: `3`;
- Build Package: `m06-fresh-accounting-interactive-study:build-package:32`;
- Build Package fingerprint: `b01ce6d13d85b1ab7663f22ef53f752e308a97cb866748b3e5cc3db109e604d8`;
- integrator candidate commit: `0230d3043540e3181ede484f7fc1eb8f85039fa1`;
- Delivery work order: `m06-fresh-founder-delivery:work-order:1`;
- final state: current candidate passed Arena's independent verifier and reached `Verified` without changing canonical acceptance or the original checkout;
- founder interventions: `1`, an owner-authorized product decision.

Research categories were UserProblem, CompetitorStatusQuo, and a bounded KnownSource prior-art/reuse check against `https://api.github.com/repos/github/github-mcp-server`. Web results were returned by OpenCode automatically and admitted through production ingestion; no operator copied a URL, summary, or verification status into Arena.

The run closed and reopened the Product OS store before package assembly. It then adopted bounded product direction, two independent architecture proposals, reuse, constraints, red-team, dissent/no-build reasoning, an owner ambiguity decision, and a real bounded GitHub risk spike. Vision, ProblemResearch, Positioning, Ambiguity, Reuse, Architecture, and BuildReadiness passed from the current package. Implementation and Release gates were not fabricated.

### Authority and Delivery regression

`opencode_adapter::tests::real_muse_authority_boundary_and_walking_skeleton` passed on rerun in **396.34 seconds** with Muse Spark. It exercised a real protected-acceptance attack in a disposable candidate, two parallel advisory roles, an integrator candidate, frozen verification, and canonical-checkout protection. The first run returned no attack diff because the model did not perform the requested mutation; it was not treated as evidence. The rerun completed the adversarial path and passed.

### Model/runtime identity

- executable: `/home/kasun/.opencode/bin/opencode`;
- OpenCode: `1.18.31`;
- model: `opencode/muse-spark-1.2-contributor-free`;
- WebDiscovery: hosted Exa through OpenCode `websearch`;
- production verifier: `websearch` only; production `webfetch` was not invoked;
- no new provider key or search credential was requested.

### Checks and package

- `cargo check`: passed with the historical 81-warning baseline;
- Product OS focused tests: 12 passed, 2 ignored, 0 failed;
- OpenCode version regression: 1 passed;
- frontend `npm run build`: passed; Vite transformed 1,712 modules;
- current package: `src-tauri/target/release/bundle/deb/Consensus Arena_0.1.0_amd64.deb`;
- package size: `10,336,576` bytes;
- package SHA256: `4202d541a307bb4d53ceb5d391a7d77944a371a4957b6204f88cb3e563187319`;
- dependencies: `libwebkit2gtk-4.1-0`, `libgtk-3-0`;
- optimized binary size: `31,462,160` bytes;
- native host script: GTK 3.24.33, WebKitGTK 2.50.4, `/dev/dri` absent, llvmpipe, no active Vite/Tauri process. Native GUI launch is `ENVIRONMENT-BLOCKED`, not PASS.

## SECURITY / AUTHORITY REVIEW

Proven through the integrated run and focused coverage:

- research prompt/web content cannot directly mutate Product Authority;
- researcher cannot self-verify; distinct FactVerifier authority is required;
- owner-required ambiguity cannot be downgraded by worker payload;
- protected acceptance mutation is detected and cannot reach canonical state;
- canonical original-checkout changes fail closed before candidate acceptance;
- cancelled/stale result and candidate/version mismatches fail closed;
- candidate changes invalidate verification identity;
- Safe Apply remains an Arena-owned explicit action;
- no credential value was printed, persisted, or added to evidence/work orders;
- the research profile did not receive repository edit, shell, local read, webfetch, MCP, or skill-install tools.

Residual boundaries remain: OpenCode permissions are not an OS sandbox; hostile same-user filesystem access is not prevented; arbitrary web-network/SSRF isolation is not complete; Git worktrees share repository object storage; and the external OpenCode executable is not installed by the package.

## WINDOWS / PACKAGING STATUS

The existing Windows workflow is the qualification path. For this exact
checkpoint SHA, run `35440202391`, the Windows frontend build completed but
`windows-build-test` stopped at the existing `cargo fmt --check` step; Windows
Rust tests, Windows `cargo check`, Job Object execution, and unsigned NSIS
packaging therefore did not run. This Linux session cannot claim Windows
runtime parity or a Windows model-backed run. The Linux `.deb` build is proven
above; package install/GUI launch is not claimed because the current host is
graphics/WebKit blocked.

## OPTIONAL / UNPROVEN

- GitHub MCP runtime use;
- ECC/gstack runtime procedures;
- shared frontier consultation execution;
- non-Zen provider runtime portability;
- OpenCode webfetch in the production research verifier;
- DSH, Dagu, and DSH/Windows composition;
- production-scale performance and multi-project throughput;
- full owner-facing GUI dogfood rather than the backend-qualified path.

## DEFERRED

- hostile same-user sandboxing and Git-object-store isolation;
- a generic research database, crawler, search engine, workflow engine, or OpenCode installer;
- broad provider benchmarking and consumer-frontier consultation;
- final release hardening beyond the bounded week-one path.

## Evidence files and changed surfaces

Source changes are limited to the OpenCode version qualification guard, truthful WebDiscovery provenance, the real ignored M06 integration exercise, the minimal journey label in `DeliveryView.tsx`, and current documentation. Historical audits and pre-existing consultation/prompt files were preserved.
