import { useEffect, useState } from 'react'
import { safeInvoke as invoke } from '@/lib/tauri'
import { useAppStore, type DeliveryState, type ProductAuthoritySnapshot } from '@/stores/useAppStore'
import Topbar from '@/components/layout/Topbar'
import FounderJourney from '@/components/shared/FounderJourney'

export default function DeliveryView() {
  const state = useAppStore(s => s.deliveryState)
  const setState = useAppStore(s => s.setDeliveryState)
  const [product, setProduct] = useState<ProductAuthoritySnapshot | null>(null)
  useEffect(() => { let disposed = false; invoke<string>('get_delivery_state').then(raw => { if (disposed) return; try { const parsed = JSON.parse(raw) as DeliveryState|null; if (parsed) setState(parsed) } catch {} }).catch(() => {}); return () => { disposed = true } }, [setState])
  useEffect(() => { let disposed = false; invoke<string>('get_latest_product_os_snapshot').then(raw => { if (disposed) return; try { const parsed = JSON.parse(raw) as ProductAuthoritySnapshot|null; if (parsed) setProduct(parsed) } catch {} }).catch(() => {}); return () => { disposed = true } }, [])
  const terminal = state?.phase === 'verified' || state?.phase === 'applied' || state?.phase === 'cancelled' || state?.phase === 'failed'
  async function apply(){try{await invoke('apply_delivery',{session_id:state?.session_id});}catch(error){useAppStore.getState().addToast(String(error),7000)}}
  async function resume(){try{await invoke('resume_delivery')}catch(error){useAppStore.getState().addToast(String(error),7000)}}
  async function abort(){if(state)try{await invoke('abort_delivery',{session_id:state.session_id})}catch(error){useAppStore.getState().addToast(String(error),7000)}}
  return <section className="view"><Topbar title={state?.objective?.slice(0,48) || 'Build'} /><div className="scroll pt" style={{display:'flex',justifyContent:'center'}}><div className="fw"><div className="fh">{state?.status_text || state?.message || 'Preparing your build…'}</div><div className="fh-sub">Arena owns acceptance and verification; worker prose never marks a build successful.</div>{state && <FounderJourney phase={state.phase}/>} {product && <div className="fg"><label className="fl">Product research</label><div style={{color:'var(--t2)',fontSize:13}}>Project revision {product.records.project_revision} · {product.records.evidence.length ? `${product.records.evidence.length} evidence record(s)` : 'No evidence yet'}</div>{product.records.evidence.slice(-3).map(item=><div key={item.evidence_id} style={{padding:'8px 0',color:'var(--t2)'}}>{item.verification === 'independently_verified' ? '✓ Independently verified' : item.verification === 'contradicted' ? 'Blocked: contradicted' : item.verification === 'unresolved' ? 'Blocked: unresolved' : 'Unverified proposal'} — {item.summary}{item.source?.title ? ` · ${item.source.title}` : ''}</div>)}{product.records.ambiguities.filter(item=>item.resolver === 'owner' && item.status !== 'resolved').map(item=><div key={item.ambiguity_id} className="form-error" role="status">Owner decision required: {item.question}</div>)}{product.research_gate && <div style={{color:'var(--t3)',fontSize:12}}>Research gate: {product.research_gate.status} — {product.research_gate.reason}</div>}</div>}{state?.build_package_id ? <div className="fg"><label className="fl">Build Package</label><div style={{color:'var(--t2)',fontSize:13}}>{state.build_package_ready ? 'Accepted package is bound to Delivery.' : 'Build Package is not current; Delivery is blocked.'}</div></div> : null}{state?.contract?.acceptance_criteria?.length ? <div className="fg"><label className="fl">This build will be considered successful when:</label>{state.contract.acceptance_criteria.map(c=><div key={c.id} style={{padding:'8px 0',color:'var(--t2)'}}>✓ {c.description}</div>)}</div> : null}{state?.work_order?.error&&<div className="form-error" role="status">{state.work_order.error}</div>}{state?.evidence?.length ? <div className="fg"><label className="fl">Evidence</label><div style={{color:'var(--t2)',fontSize:13}}>{state.evidence[state.evidence.length-1].summary}</div></div> : null}{state && <div className="fg" style={{color:'var(--t3)',fontSize:12}}>Attempt {state.attempt} · {state.branch_name || 'isolated candidate'}{state.verification_summary ? ` · ${state.verification_summary}` : ''}</div>}{state?.phase === 'verified' && <button className="btn-p" onClick={()=>void apply()}>Apply</button>}{state?.phase === 'applied' && <div className="form-success">Candidate applied safely to the original branch.</div>}{terminal && state?.phase !== 'verified' && state?.phase !== 'applied' && <button className="btn-p" onClick={()=>void resume()}>Resume build</button>}{!terminal && <button className="sv-btn" onClick={()=>void abort()}>Stop build</button>}</div></div></section>
}
